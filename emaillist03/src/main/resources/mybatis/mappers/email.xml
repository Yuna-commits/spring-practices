<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="email">
	<insert id="insert" parameterType="emailvo">
		<![CDATA[
			insert 
			into email(first_name, last_name, email, reg_date)
			values (#{firstName}, #{lastName}, #{email}, current_date())
		]]>
	</insert>
	
	<delete id="deleteById" parameterType="long">
		<![CDATA[
			delete 
			from email 
			where id = #{id}
		]]>
	</delete>
	
	<select id="findById" parameterType="long" resultType="emailvo">
		<![CDATA[
			select id, date_format(reg_date, '%Y-%m-%d') as regDate
			from email 
			where id = #{id}
		]]>
	</select>
	
	<select id="findAll" resultType="emailvo">
		<![CDATA[
			select id, first_name as firstName, last_name as lastName, email
			from email 
			order by id desc
		]]>
	</select>
</mapper>